export const API_VERSION = "v01.02.00";
export const API_VERSION_DISPLAY = "v1.2.0";

{/* 
  API Version Configuration
  
  To update the API version across all guides:
  1. Change API_VERSION above to the new folder name (e.g., "v01.03.00")
  2. Change API_VERSION_DISPLAY to the human-readable version (e.g., "v1.3.0")
  
  Usage in guide files:
  
  import { API_VERSION } from "/snippets/api-version.mdx";
  
  Then use in links:
  [Get Signing Request](../api-reference/{API_VERSION}/signing-requests/get-signing-request)
*/}
